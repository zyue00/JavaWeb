server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cms_db?useSSL=false&serverTimezone=UTC
    username: root
    password: zq5294890899
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10

  mybatis-plus:
    mapper-locations: classpath:/mapper/*.xml
    type-aliases-package: com.gzu.javawebproject.entity
    configuration:
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    root: info
    com.gzu.javawebproject: debug

devtools:
  restart:
    enabled: true
